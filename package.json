{
  "name": "reverse-proxy",
  "version": "1.0.0",
  "description": "`http-proxy-middleware` to serve a reverse proxy that routes to multiple `express` applications as containers via `docker-compose`",
  "main": "index.js",
  "scripts": {
    "test": "exit 0"
  },
  "author": "Conrad Johnston <conrad@darnoc.co.nz>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nzcodarnoc/korerorero-reverse-proxy.git"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "envalid": "^6.0.2",
    "express": "^4.17.1",
    "greenlock-express": "^4.0.3",
    "helmet": "^3.22.0",
    "http-proxy-middleware": "^1.0.3"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
